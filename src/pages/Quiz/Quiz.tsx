import React from 'react'
import QuizPage from '../../components/screens/QuizPage/QuizPage'
import { observer } from 'mobx-react-lite'

const Quiz = () => {
  return (
    <><QuizPage /></>
  )
}

export default observer(Quiz)